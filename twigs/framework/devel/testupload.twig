{% extends '@content/page.twig' %}

{% import '@util/formmacro.twig' as f %}
{% import '@util/modalmacro.twig' as m %}

{% block headerbody %}
    <h1 class="text-center">Test File Upload and Download</h1>
{% endblock headerbody %}

{% block main %}
    <section class="row">
        <article class="mx-auto col-md-10 mb-5">
            {% include '@util/message.twig' %}
            {{f.startform({method: 'post', multi: TRUE})}}
                {{f.file({id: 'f0', name: 'upload', label: 'File'})}}
                {{f.checkbox({labels: ['Public'], values: [1], names: ['public']})}}
                <div class="mt-4">
                    {{f.submit({value: 'Upload'})}}
                </div>
            {{f.endform()}}
        </article>
    </section>
    {% if download is defined %}
        <section class="row">
            <article class="mx-auto col-md-10 mb-5">
                <p class="class="text-center mt-4">
                    <a class="btn btn-primary" href="{{base}}/devel/test/upload/get/{{download}}">Download</a>
                </p>
                <p class="class="text-center">
                    <a class="btn btn-primary" href="{{base}}/devel/test/upload/delete/{{download}}">Delete</a>
                </p>
            </article>
        </section>
    {% endif %}
{% endblock main %}

{% block pagefooter %}
{% endblock pagefooter %}
