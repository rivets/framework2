{% set title = fwurls.sitename.value|upper ~ ': ' ~ heading %}

{% extends '@content/page.twig' %}

{% import "@content/iconmacro.twig" as i %}

{% block header %}
    <section class="col-md-12 mt-5 text-center">
        <h1 class="text-center">{{heading}}</h1>
    </section>
{% endblock header %}

{% block main %}
    <section class="row mt-4">
        <article class="ml-auto col-lg mr-auto text-center">
            <div class="jumbotron">
                {% if left %}
                    <div class="text-left">{{raw ? message|raw : message}}</div>
                {% else %}
                    <p>{{message}}</p>
                {% endif %}
            </div>
            <p><a href="{{base}}/" class="btn btn-primary">{{i.fa('home')}} Home Page</a></p>
        </article>
    </section>
{% endblock main %}

{% block pagefooter %}
{% endblock pagefooter %}
